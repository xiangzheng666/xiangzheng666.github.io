---
layout: post
author: liuxiangzheng
categories: study
---

# Project

- 作为第三方bean自动配置

  - ```
    @EnableConfigurationProperties({
            SmsProperties.class
    })
    public class TanhuaAutoConfiguration {
        @Bean
        public SmsTemplate smsTemplate(SmsProperties smsProperties) {
            return new SmsTemplate(smsProperties);
        }
    }
    
    META-INF/spring.factories
    org.springframework.boot.autoconfigure.EnableAutoConfiguration=\
    com.tanhua.autoconfig.TanhuaAutoConfiguration
    说明第三发bean的位置
    
    @Import(TanhuaAutoConfiguration.class)
    ```

- 注入yaml文件的属性

  - ```
    @Value(${})
    @ConfigurationProperties(prefix = "tanhua.sms")
    ```

  - @ConfigurationProperties

    - 可以将yaml中同名的属性注入类，但是该类并不加入ioc容器中
    - 还需在使用该bean类上加@EnableConfigurationProperties({class_name})
    - 或者在该类加入@Componet

- ResponseEntity的使用

  - ```
    import org.springframework.http.ResponseEntity;
    ResponseEntity.ok(body); 封装响应
    响应体，响应头。。。
    ```

    